pydantic>=2.0,<3.0
click>=8.1,<9.0
rich>=13.0,<14.0
numpy>=1.24
scipy>=1.10
psutil>=5.9
pillow>=11.3        # AVIF encode/decode (libavif wheels) — image + page modes

# Optional — only needed for on-chain proof anchoring (nebula/receipt.py):
#   base58           (PDA derivation)
#   solana, solders  (direct Solana submit; falls back to Solana CLI / Node if absent)

# External tools required on PATH for the VIDEO paths only (not pip-installable):
#   ffmpeg >= 7.1 built with --enable-libvmaf
#   libvmaf >= 3.0.0
#   x265 >= 3.6 or SVT-AV1 >= 2.0
# The image + page modes need no ffmpeg — Pillow's bundled libavif does the work.
#
# Install from source: bash scripts/install_deps.sh
